<!-- The squash commit takes this PR's title, so make the title the message you want in history. -->

## What and why



## Checklist

- [ ] `npm run typecheck && npm run lint && npm test` pass locally
- [ ] `CHANGELOG.md` has a line under `[Unreleased]` if this is user-facing
- [ ] README table and `--help` updated if a flag, env var or config key changed
- [ ] If a string the model sees changed (tool description, schema, error), the old and new text are in this description
- [ ] `test/fixtures/bookshop.xml` regenerated if `examples/bookshop` changed
